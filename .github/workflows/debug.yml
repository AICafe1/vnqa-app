name: debug

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: robusta
        uses: fawazahmed0/action-debug-vscode@main
        env:
          PASS: ${{ secrets.PASS }}
